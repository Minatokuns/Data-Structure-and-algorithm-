reverse a integer:::::
while(x>0){
            rev =rev*10 + x%10;
            x = x/10;
        }
 no is power of two::::::
         static boolean isPowerOfTwo(int n)
    {
        if (n == 0)
            return false;
 
        while (n != 1) {
            if (n % 2 != 0)
                return false;
            n = n / 2;
        }
        return true;
    }
